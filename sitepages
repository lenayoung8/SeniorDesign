Potential pages for LAN Lord
- Login page - looks like a regular login page with username and a password fields and login button. Also has a button to sign up.
- Signup page - where a user can enter their username, password, and email. When the user signs up or logs in, it takes them to a homepage.
- Homepage - just like what we have, but with an extra view for network information. If there's no network attached to an account, no information shows.
- Network page - a place where a user can either register a network or register to be a part of an existing network. If they register a network, then they have to enter a name, maybe a password (or maybe not for security concerns?) and the IP and MAC address on the router. To account for existing networks that use default router IP addresses, it uses the MAC address as an identifier; if a network with the same MAC address as a preexisting one is put in, it prompts the user to join the preexisting network (without giving the code). Once in, it pings the router to guarantee that it exists (stretch goal: verification for the router to ensure it's really the user's) and if not it prompts the user to double-check, but if so it logs it in the database and registers the network with the user account and automatically sets the user as the admin and the owner. It could possibly generate a unique code to be able to register with the network. To register with a preexisting network, a user has to enter a code and this sends a ping to the owner of the network (I could look through the networks in the user table and find the user associated with the matching name and IP and MAC addresses with the owner flag). The owner can either decline it or it can accept it and mark the user as 
Device registration - In the device view, maybe there could be a button (only admin-usable and possibly password-protected!) that can manage devices. You can add devices to the network with a unique name, device type, and IP and MAC addresses. There's also a tab to edit device information or remove a device. If a device is removed, the admin has the option to just remove it from the network or to blacklist it. (one of the ways we could flag anomalous activity is if a device not whitelisted tries to connect x number of times or a blacklisted device shows up at all)
- Stretch goals: allow users to be a part of multiple networks. In the network view there would be a toggle to change which network to view. Could possibly add a settings page.
